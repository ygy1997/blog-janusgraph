import { EdgeConfig, GraphData, NodeConfig } from '../types';
interface Cluster {
    id: string;
    nodes: NodeConfig[];
}
interface ClusterData {
    clusters: Cluster[];
    clusterEdges: EdgeConfig[];
}
/**
 * @param callbacks
 */
declare const labelPropagation: (data: GraphData, directed?: boolean, weightPropertyName?: string, maxIteration?: number) => ClusterData;
export default labelPropagation;

import { EdgeConfig, GraphData, NodeConfig } from '../types';
interface Cluster {
    id: string;
    nodes: NodeConfig[];
    sumTot?: number;
}
interface ClusterData {
    clusters: Cluster[];
    clusterEdges: EdgeConfig[];
}
/**
 * @param callbacks
 */
declare const louvain: (data: GraphData, directed?: boolean, weightPropertyName?: string, threshold?: number) => ClusterData;
export default louvain;
